SYSTEM {
    link sensor1 to actuator1;
    this.knobs.threshold = 42;
    foreach idx in wholeindices {
        if (idx < 3) {
            this.sensors.temperature = idx * 10;
        }
    }
}

// ne compile pas parce que fait ref à 
// des éléments non definis (sensor1, actuator1, ...)