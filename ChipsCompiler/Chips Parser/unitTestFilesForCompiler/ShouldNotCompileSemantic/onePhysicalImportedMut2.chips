import "Hello.json" as aBlock;

physical aBlock(int intflow) init {
    int aVar = 0;
} then {
    aVar = aVar + intflow;
} -> (aVar/intFlow)

pure input() -> (t)

pure output(int dataflow) -> ()

SYSTEM dimensions(3) {
    aBlock myBlock;
    // myBlock at (1,2,3);
    myBlock.in(input());
    output(myBlock.out);
}


// /!\ dans l'ancienne version de Chips,
// je considérais "t" et "dt" comme des 
// mots réservés, d'où la ligne 9

// je crois que dans l'ancienne version de Chips, 
// je voulais imposer que tous les blocks fonctionnels 
// "Physical" aient une coordonée dans l'espace
// donc il est possible que j'eu considéré nécéssaire
// que la ligne 15 ne soit pas commentée.

// En soit, ce n'était pas une mauvaise idée, 
// mais ce n'est clairement pas la priorité
// d'où l'absence de l'instruction "at" dans la 
// prochaine version